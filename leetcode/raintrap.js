/**
 * @param {number[]} height
 * @return {number}
 */
var trap = function(height) {
    let ret = 0;

    for(let i = 0; i < height.length - 1; i++) {
        for(let j = i + 1; j < height.length; j++) {

        }
    }

    return ret;
};
